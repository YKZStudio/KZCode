# 2026-06-13

## 删除多余语言文件（保留 en/zh/zht）
- 删除了 5 个 i18n 目录中所有非中英文的语言文件（共 73 个 .ts/.json 文件）
- 删除了 15 个非中英文的文档翻译目录 (packages/web/src/content/docs/)
- 更新了 6 个配置文件移除已删除语言的引用：
  - packages/app/src/context/language.tsx
  - packages/console/app/src/i18n/index.ts
  - packages/console/app/src/lib/language.ts
  - packages/desktop/src/renderer/i18n/index.ts
  - packages/web/src/i18n/locales.ts
  - packages/app/src/i18n/parity.test.ts

## TUI 全量接入 i18n
- 在 TuiConfig 中添加了 `locale` 字段（en/zh/zht），支持从环境变量 LANG/LC_ALL 自动检测
- 创建了 i18n 翻译基础设施：packages/tui/src/i18n/{index.ts, en.ts, zh.ts}
- 使用"英文字符串作为 key"的模式，自动回退到英文
- zh.ts 包含 716 个翻译条目，覆盖所有用户可见字符串
- 更新了 42 个 TUI 源文件，所有硬编码英文字符串替换为 t() 调用
- 中文用户设置 tui.json 中 locale: "zh" 即可切换语言

## TUI 添加 /language 命令
- i18n/index.ts 重构为响应式：locale 改用 SolidJS signal，t() 函数内调用 localeSignal()
  使得切换语言时所有组件自动重渲染
- app.tsx 新增 language.switch 命令，slashName 为 "language"
- 语言循环顺序：en → zh → zht → en
- 切换后持久化到 KV store，重启后保持
- 初始化时优先读取 KV 中的 locale，其次 config，最后环境变量

## TUI i18n 遗漏修复
- autocomplete.tsx: 斜杠命令描述和"No matching items"添加 t() 翻译
- diff-viewer.tsx: 全面接入 i18n（命令标题、帮助文本、操作按钮）
- 修复 zh.ts 中 identity 映射：agents→智能体, commands→命令, build→构建, plan→计划, native→内置
- 补充 50+ 条缺失翻译（命令描述、差异查看器 UI、帮助文本等）

## 语言自动检测（已有功能确认）
- Web/Desktop: detectLocale() 通过 navigator.language 自动检测，中文系统默认用中文
- TUI: detectSystemLocale() 通过 LANG/LC_ALL 环境变量自动检测
- 检测逻辑：zh 开头 → 简体中文，zh-Hant/zh-TW → 繁体中文，其他 → 英文
